{
  "homepage": "https://deruvomichele-cloud.github.io/ash",
  "name": "ash-swap-dapp",
  "version": "1.0.0",
  "description": "ASH Swap dApp - USDC ↔ ASH exchange on Base",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "ethers": "^6.10.0"
  },
  "devDependencies": {
    "react-scripts": "5.0.1",
    "gh-pages": "^6.0.0"
  },
  "overrides": {
    "ajv": "^8.12.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
